name: Deploy to VPS

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy over SSH
        uses: appleboy/ssh-action@v1.2.2
        with:
          host: ${{ secrets.VPS_HOST }}
          port: 22
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          password: ${{ secrets.VPS_PASSWORD }}
          timeout: 180s
          command_timeout: 30m
          script: |
            set -euo pipefail
            export APP_DIR="${{ secrets.VPS_APP_DIR || '/opt/mercado-ahora' }}"
            export REPO_URL="https://github.com/Taras-Progo/Mercado-Ahora.git"
            export BRANCH="main"
            export APP_URL="${{ secrets.APP_URL }}"
            export FRONTEND_URL="${{ secrets.APP_URL }}"
            export APP_DOMAIN="${{ secrets.APP_DOMAIN }}"
            export ACME_EMAIL="${{ secrets.ACME_EMAIL }}"
            export MAIL_MAILER="${{ secrets.MAIL_MAILER }}"
            export MAIL_HOST="${{ secrets.MAIL_HOST }}"
            export MAIL_PORT="${{ secrets.MAIL_PORT }}"
            export MAIL_SCHEME="${{ secrets.MAIL_SCHEME }}"
            export MAIL_USERNAME="${{ secrets.MAIL_USERNAME }}"
            export MAIL_PASSWORD="${{ secrets.MAIL_PASSWORD }}"
            export MAIL_FROM_ADDRESS="${{ secrets.MAIL_FROM_ADDRESS }}"
            export MAIL_FROM_NAME="${{ secrets.MAIL_FROM_NAME }}"
            export MERCADO_PAGO_MODE="${{ secrets.MERCADO_PAGO_MODE }}"
            export MERCADO_PAGO_PUBLIC_KEY="${{ secrets.MERCADO_PAGO_PUBLIC_KEY }}"
            export MERCADO_PAGO_ACCESS_TOKEN="${{ secrets.MERCADO_PAGO_ACCESS_TOKEN }}"
            export MERCADO_PAGO_SUCCESS_URL="${{ secrets.MERCADO_PAGO_SUCCESS_URL }}"
            export MERCADO_PAGO_PENDING_URL="${{ secrets.MERCADO_PAGO_PENDING_URL }}"
            export MERCADO_PAGO_FAILURE_URL="${{ secrets.MERCADO_PAGO_FAILURE_URL }}"
            export MERCADO_PAGO_WEBHOOK_URL="${{ secrets.MERCADO_PAGO_WEBHOOK_URL }}"
            export MERCADO_PAGO_WEBHOOK_SECRET="${{ secrets.MERCADO_PAGO_WEBHOOK_SECRET }}"

            if [ ! -x "$(command -v docker)" ]; then
              curl -fsSL https://raw.githubusercontent.com/Taras-Progo/Mercado-Ahora/main/deploy/provision-ubuntu.sh -o /tmp/provision-mercado.sh
              bash /tmp/provision-mercado.sh
            fi

            mkdir -p "$APP_DIR"
            if [ ! -d "$APP_DIR/.git" ]; then
              git clone --branch "$BRANCH" "$REPO_URL" "$APP_DIR"
            fi
            cd "$APP_DIR"
            git fetch origin "$BRANCH"
            git reset --hard "origin/$BRANCH"
            bash deploy/deploy.sh
